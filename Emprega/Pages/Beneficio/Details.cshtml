@page
@model Emprega.Pages.Beneficio.DetailsModel

@{
    ViewData["Title"] = "Detalhes";
}

<h1>Detalhes</h1>

<div>
    <h4>Beneficio</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Beneficio.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Beneficio.Nome)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Beneficio.Id">Editar</a> |
    <a asp-page="./Index">Voltar para a lista</a>
</div>
